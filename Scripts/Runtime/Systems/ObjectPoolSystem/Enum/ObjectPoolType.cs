namespace Tolian.Runtime.Systems.ObjectPoolDomain
{
   /// <summary>
   /// This enum can be extended according to needs of object pools. Each enum value can be used to create a pool in ObjectPoolSystem which can be accessed via ManagerObjectPool
   /// </summary>
   public enum ObjectPoolType 
   {
      DefaultObject = 0,
      UiCoin
   }
}

